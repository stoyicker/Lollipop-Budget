language: android
android:
  components:
    - build-tools-21.1.1
branches:
  only:
    - master
before_install:
# Signature stub start
    - echo "stub keystore" > lbudget.keystore
    - echo "stub storage pwd" > lbudgetstorage.pwd
    - echo "stub key pwd" > lbudgetkey.pwd
# Signature stub end
    - chmod +x ./gradlew
script:
    - ./gradlew clean assembleReleaseTravis
